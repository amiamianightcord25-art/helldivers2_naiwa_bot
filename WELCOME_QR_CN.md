# 欢迎提示与可选二维码

## 触发条件

- 机器人被加入群时，收到 `GROUP_ADD_ROBOT` 事件后回复介绍。
- 用户添加机器人为好友时，收到 `FRIEND_ADD` 事件后回复介绍。
- 普通 QQ 私聊和频道私信首次收到消息时补充指引，已有欢迎记录的会话不重复补充。

“入群”指机器人加入群，不是普通群成员入群。打开已有聊天窗口本身没有可靠事件；程序启动或重启不会向已有群、好友群发欢迎语。历史或缺少事件 ID 的添加事件不触发回复。

欢迎去重按机器人 AppID、场景和目标保存，记录保留约一年。普通查询消息另有内存去重，重启会清空；两者不是同一套记录。

## 欢迎内容

介绍包含常用查询、菜单、签到、战备英雄、百科选择及开源说明；群介绍另含推送入口。最新原文以 [welcome.py](src/hd2bot/qq/welcome.py) 为准，本文不重复整段文案。

## 可选图片二维码

公开版不附带运营者二维码。需要展示时，将自己的 PNG 放在本地 `data/qq_experience_qr.png`，或在 `.env` 的 `HD2_QR_IMAGE_PATH` 指定文件，修改后重启机器人。没有可读二维码时，卡片不显示该区域，其他内容照常渲染。

百科、星图、战况等共用图片页脚。配置后右下角显示原图及“QQ扫一扫体验”，不重新生成二维码内容，不从外链加载。1200 像素宽图片中二维码框为 192×192 像素（含白边）；框大小随输出宽度调整至 176～220 像素。

中文及 emoji 字体安装见 [Linux 部署说明](DEPLOYMENT_CN.md)。自动化测试覆盖欢迎事件、去重、二维码页脚及缺图渲染；QQ 客户端展示和扫码结果仍需在具体部署中验证。
