# 签到、快捷菜单与战备英雄

## 使用入口

普通 QQ 私聊和频道私信直接发送指令；群聊、文字子频道先 @机器人。

| 功能 | 指令 |
| --- | --- |
| 每日签到 | `签到` / `打卡` |
| 等级与称号 | `我的等级` / `等级表` |
| 当前会话签到排行 | `签到排行` |
| 分类菜单 | `菜单` / `指令面板` |
| 菜单分类 | `菜单 战情` / `菜单 百科` / `菜单 生涯` / `菜单 娱乐` |
| 开始战备小游戏 | `战备英雄` / `随机战备` |
| 游戏状态与说明 | `战备英雄 状态` / `战备英雄 帮助` |
| 结束游戏 | `结束战备` / `结束游戏` |
| 最高分与排行 | `战备记录` / `战备排行` |
| 游戏加载小贴士 | `小贴士` / `小贴士 <编号>` / `小贴士 来源` |

所有指令支持 `/` 前缀。原生面板点选或消息按钮可能只填入输入框，用户需要点击发送。

## 签到规则

北京时间每日 00:00 刷新，每日最多领取一次。第一天 100 经验，连续签到每天多 10，第七天起每天 160；中断后从 100 重新开始，累计经验保留。从等级 L 升到 L+1 需要 `100 + 2 × (L−1)` 经验；达到等级 L 的累计门槛为 `100 × (L−1) + (L−1) × (L−2)`。300 级后继续累计签到天数，不再增加经验。

签到回执使用 Markdown，附“我也要签到”按钮；其他群成员点击后可发送填入的签到指令，为自己签到。同日重复操作不重复领取经验。平台明确拒绝 Markdown 时保留文字回执。

称号按 HD2 等级解锁顺序递增，共 36 档，最高 300 级；不含付费或战争债券专属称号。游戏中的 140 级“列兵”位于 130 级“十星将军”之后，这是原游戏的命名顺序。全部中文称号已按 1920×1080 游戏实拍核对：1–150 级来自 2024-05-05 录像，160–300 级来自 2026-08-12 录像。290 级和 300 级保留游戏原文“无限最高等级+1”“300级”。完整数据、来源及视频时间点在 `src/hd2bot/assets/rank_titles.json`；对照记录见 [称号核对](research/rank_verification_20260919.md)。

这是机器人签到等级，与游戏账号经验独立。不同 QQ 聊天类型的用户标识分别累计；同一类型内同一个平台用户标识跨群签到不会重复领奖。群和子频道的榜单只展示在该会话签到的成员；私聊只展示本人。平台没有提供昵称时显示“未提供昵称”。

签到状态与小游戏记录保存到 `DATABASE_PATH` 指定的 SQLite 数据库；重启保留。独立表按需创建，不改动现有游戏消息订阅。

## 战备英雄

参考舰船上的 Stratagem Hero 和公开社区复刻，使用本地 Wiki 中可验证的中文战备及真实方向序列，目前题库 50 项。

每关 120 秒，首关 5 题，后续逐关增加至每关最多 12 题。答对一题补 8 秒，剩余时间最多 120 秒。完整答对后按每个方向 5 分结算，过关另有时间、关卡和完美奖励。输错清空当前题的输入进度并失去本关完美奖励；无法识别的字符会提示，不静默忽略。聊天版计时已为 QQ 往返与输入法放宽。

支持一次发送完整序列，也支持逐条输入。例如同一序列可以输入：

```text
↑ ↓ ← →
⬆️⬇️⬅️➡️
＾ｖ＜＞
ＷＳＡＤ
上下左右
[上][下][左][右]
👆👇👈👉
```

另支持多种 Unicode 单向箭头、英文方向词和 `->` / `<-`。斜箭头和双向箭头含义不唯一，会要求重新输入。`DSS` 优先作为空间站查询指令；需要输入“右下下”时可用 `→↓↓`。

每位用户、每个会话独立一局；重启继续使用原截止时间。超时在下次操作时结算，不主动发消息催促。最高分在游戏结束或超时结算后进入当前会话排行。

## 游戏加载小贴士

发送 `小贴士` 随机获得一条加载界面提示，附“再来一条”按钮。同一会话连续抽取不会立即重复上一条；可以发送 `小贴士 1` 至 `小贴士 83` 查看固定编号，`小贴士 来源` 查看收录说明。群聊与频道先 @机器人，普通私聊与频道私信直接发送。

内置收录 83 条《HELLDIVERS 2》加载提示，来自 Helldivers Wiki 的 [Training Manual Tips 固定修订 108731](https://helldivers.wiki.gg/wiki/Training_Manual_Tips?oldid=108731)，包括世界观、任务、战略配备、战斗和通用提示。页面明确为二代投入任务期间显示的文字，未混入普通攻略或一代提示。

中文目前为人工整理的参考译文，回复中逐条标注；未冒称已核对游戏客户端官方简中文字串。每条英文原句、来源和翻译状态保存在 `src/hd2bot/assets/loading_tips.json`。收录与译文按 [CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/) 署名，原始游戏文字属于 Arrowhead / Sony。

## Markdown 与操作按钮

DSS、战局查询、百科翻页/目录、帮助和更新日志也在有明确下一步时附按钮。已有签到、菜单和战备英雄按钮保留。

百科翻页按钮对应原查询者的候选列表，群聊和频道仅原查询者可使用；其他成员可点击不受此限制的“百科菜单”打开自己的列表。

有图片时先发送图片，再附短 Markdown 操作消息。群聊按钮只填入指令，点击发送后执行；普通单聊可直接发送。

输出考虑腾讯限制：群每条被动消息最多 5 次回复，普通单聊最多 4 次；本地 Markdown 正文保守限制为 1800 UTF-8 字节，较长正文先分段发文字，再附短按钮，不把此本地阈值宣称为腾讯公布的上限。消息按钮采用兼容上限：最多 5 行、每行最多 5 个、标签最多 10 字符；自定义分类菜单另限制每行最多 3 个。这些约束与原生底部菜单/指令面板的限制不同。频道若未开放 Markdown，或平台明确拒绝格式/链接，按错误类型回退为文字；发送结果不明时不重复补发图片或 Markdown。详见 [官方限制核对](research/qq_markdown_limits_20260919.md)。

文字子频道发送在本进程中统一间隔至少 0.21 秒，满足同频道每秒最多 5 条的限制。某条回复中的链接首次明确被拒绝后，后续分段直接省略链接，保留正文、缩略说明和可用操作，避免重复消耗回复次数。

## 菜单配置与维护

原生入口使用腾讯现行官方 API：普通私聊底部菜单 `/v2/menu`；普通私聊、群聊、文字子频道、频道私信的快捷指令 `/v2/panels`。腾讯原生底部菜单仅支持普通私聊；其余三种场景使用原生指令面板及发送 `菜单` 的入口。

```powershell
# 纯本地校验
.\.venv\Scripts\python.exe -X utf8 scripts\configure_qq_ui.py validate
# 对比线上配置，只读
.\.venv\Scripts\python.exe -X utf8 scripts\configure_qq_ui.py plan
# 应用配置，并重新读取验证结果
.\.venv\Scripts\python.exe -X utf8 scripts\configure_qq_ui.py apply --backup data\native-ui-before.json
```

工具优先读取不提交到 Git 的 `data/qq_native_ui.json`，也可通过 `--config` 指定文件。公开的 `qq_native_ui.example.json` 是不产生修改的空白结构；配置后才会新增或修改自己命名的面板，不删除其他面板。

机器人回复菜单独立读取 `data/menu.json`；不配置时使用程序内置的通用功能菜单 `src/hd2bot/assets/command_menu.json`。`menu.example.json` 是空白起点，不能不填写就作为有效菜单使用。配置须有 `首页`，总共 1～8 页；每页有 `title` 和 1～5 行 `rows`，每行 1～3 个含 `label`、`command` 的按钮。页面整体替换内置菜单，不是逐项合并；可参考内置通用菜单的结构。保存有效文件后下次查询生效；无效修改保留最近有效配置。删除文件不会立即恢复默认菜单，重启后恢复。消息内按钮被平台明确拒绝时回退为文字指令列表。

资料：[QQ 官方菜单与面板](https://bot.q.qq.com/wiki/develop/api-v2/server-inter/menu-panel/)、[签到研究](research/checkin_feature_20260919.md)、[游戏玩法研究](research/stratagem_hero_20260919.md)、[官方配置接口核验](research/qq_menu_20260919.md)。
