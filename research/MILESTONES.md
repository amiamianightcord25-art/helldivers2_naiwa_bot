> 历史记录：按时间追加的阶段记录。下文“当前”“本轮”、测试数量和运行状态均指记录时点，不是现行部署说明。当前使用方法见 [项目 README](../README.md)，文档范围见 [研究索引](README.md)。

# 开发与验证记录

当前用户消息优先于粘贴需求：Windows 本地开发，暂不部署服务器或 Docker。

## M0 项目初始化

- 建立 Python src 项目、虚拟环境、环境配置、脱敏日志及 Git 忽略规则。
- 使用本机 Python 3.14.4，项目声明最低 Python 3.11。
- `run.py --version`、启动、`pip check` 通过。
- 系统临时目录权限不允许 pytest 写入，已配置 `tmp/pytest`。
- M0 pytest: 2 passed。

## M1 API 研究
- 官方公开接口 5/5、社区整理接口 4/4 HTTP 200。
- 已记录抓包证据、字段与相对时间/语言/认证差异，详见 API_NOTES.md。
- pytest: 2 passed。
- 用户指定社区 X-Super-Contact 为 example@example.com。

## M2 数据核心
- 完成统一模型、官方公开/社区/mock provider、失败切换、TTL请求合并、429/超时/坏JSON处理。
- 真实smoke：auto(官方)/community/mock 各5个数据方法全部通过，真实数据包含273星球、34战役、1主线。社区头使用 example@example.com。
- 曾出现社区瞬时连接超时；其后同客户端恢复200，错误已被统一异常隔离。
- 修复跨命令双层缓存虚增采样时间；保留真实抓取时刻与剩余TTL。
- pytest: 78 passed；ruff与pip check通过。
- 测试使用合成HTTP响应；已适配SDK要求的aiohttp3.14与aioresponses兼容差异。

## M3 CLI 与中文命令
- 完成共同router、parser、中文formatter与交互CLI；中英名/编号/模糊候选均可用。
- 真实CLI连续执行战况、主线、Meridia、天使之愿、进攻、防守、玩家、错误命令、帮助和退出，退出码0；6类查询总共仅5次官方HTTP。
- 同序列mock完成，HTTP请求0，回复明确标识模拟。
- API失败/坏输入后仍可继续查询；支持 --command 单次查询。
- pytest: 188 passed；ruff通过。
- 本地联调输出留在忽略的tmp/cli_auto.txt与tmp/cli_mock.txt。

## M4 QQ 官方平台适配器
- 按已安装qq-botpy-sdk2.0.3实际能力实现webhook/websocket，共用CLI router；默认只监听127.0.0.1。
- 支持群@与C2C被动回复，msg_id/seq、分段、去重、签名新鲜度与AppID检查、并发边界和安全关闭。
- SDK日志正文不透传，动态token/secret脱敏；无效sandbox配置拒绝启动。
- 本地临时HTTP端口完整验证签名→SDK→router→mock数据→模拟QQ回复；challenge、错误签名、重复事件通过；对真实QQ网络请求0。
- pytest: 218 passed；ruff通过；scripts/qq_local_probe.py通过。
- 未提供AppID/AppSecret，真实QQ沙箱/线上消息尚未验证，不能标记PASS。

## M5 SQLite 持久化
- CLI/QQ启动会自动初始化可配置SQLite路径，三张业务表已在data/bot.db实际建立。
- 订阅CRUD有目标隔离和唯一约束；JSON设置参数绑定，事务失败回滚。
- 通知指纹SHA256跨重启保留，比较与成功标记分离；无主动推送。
- pytest: 244 passed（含26个存储测试）；ruff通过。
- 用户随后提供QQ凭据并指定websocket；已写入被Git忽略的.env，进入真实连接验证。

## M6 健壮性与最终验收
- 修复并行请求失败遗留任务：取消并等待sibling，防止关闭HTTP session后仍持锁请求。
- 修复429冷却期间待发请求的竞态：节流等待醒来后重新检查。
- 战争开始/结束日期影响状态显示，保留mock标识。
- 新增15项健壮性回归及403/404映射验证；完整pytest 261 passed；ruff/pip check与本地Webhook复验通过。
- 最终真实CLI六类命令6/6，5次官方HTTP，正常退出；Python3.11语法检查通过。
- 用户指定websocket：实际QQ鉴权、资料、Gateway API均200；真实连接READY成功。最终版本已在本地后台运行。
- 收尾时已观察真实C2C事件（01:13:08），官方API查询成功，并于01:13:11成功调用QQ API回复1段；群聊仍待实测，详见ACCEPTANCE.md。
- 实际AppSecret不在可提交文件或已有6个Git提交的历史中。

## 本地启动与关闭脚本（2026-09-13）
- 增加start_bot.cmd、stop_bot.cmd、status_bot.cmd，支持双击及--no-pause调用。
- QQ入口使用系统文件锁避免重复运行，运行记录绑定随机实例；不使用陈旧PID强制杀进程，CLI可独立运行。
- 正常关闭取消并等待QQ/HTTP/SQLite清理，之后释放锁；后台控制台日志追加保留。
- 实际Windows验证：启动收到WebSocket READY，重复启动不创建另一业务实例，停止清理后无残留bot进程，重复停止安全。
- 新增12项运行管理测试；完整pytest 273 passed，Ruff和pip check通过。

## NoneBot2 与 HTML/JPEG 查询图（2026-09-16）
- 按用户新要求迁移为NoneBot2 2.5.0 + QQ官方适配器1.7.2，移除botpy与旧Webhook实现；保留CLI与数据核心。
- 参考Tenko历史BF1三列布局及通用HTML工具，原创HD2模板由Jinja/Chromium截图后JPEG压缩。无第三方图片字体复制。
- 战况/星球详情默认图；帮助/错误/短结果文字。渲染、上传失败文字回退。
- 借鉴GWW新增新闻/战报、补给线查询，官方NewsFeed与社区dispatches实测成功。
- 全套379tests通过后补充READY/连接超时/WS接收超时回归；当前QQ/渲染/卡片/情报87项通过，Ruff/pip check通过。
- 真实NoneBot框架WebSocket READY通过，发现上游30秒receive小于QQ心跳导致重连，已按QQ适配边界改120秒并有实驱动参数回归。
- 实图生成和目视检查：真实银河战况约132KB；QQ官方API的Base64上传及被动media参数已离线完整验证。
- 实际QQ图片送达仍待用户触发查询，不能把离线上传模拟视为平台送达。


## 主动推送与订阅（2026-09-16）
- 完成 SQLite v2 迁移，增加定时战况、主线、防守、星球订阅的运行状态、基线、退避与发送记录。
- 仅允许 `.env` 指定 QQ 号通过一次性私聊绑定码配对；群聊、其他私聊和未绑定目标均在命令、轮询、发送三层拒绝。
- 主动消息使用 QQ 官方无被动 ID 接口；成功后才推进指纹、额度和下次时间，平台拒绝会暂停订阅。
- 接入 NoneBot 连接生命周期，READY 后启动轮询，断开时禁止发送，关闭时取消任务；普通查询仍使用原有被动回复。
- 新增订阅、配对、额度、并发竞态和 QQ 发送边界测试；当前完整 pytest 424 passed，Ruff、pip check、compileall 通过。
- 真实 QQ C2C 查询链路已有成功记录；主动推送和沙箱群聊仍需用户在 QQ 客户端触发验证。

## GWW 星图与公共功能扩展（2026-09-16）

- 核对用户指定的当前 README 和本地参考源码，完成全部命令/通知形态的覆盖矩阵。
- 新增银河/局部星图：真实坐标、阵营、战役、防守、明确主线目标、无向补给线、紧凑编号避让。
  沿用自包含 HTML/SVG→Chromium→JPEG；保留文字备用、来源/采样/模拟/陈旧标识。
- 新增阵营战线（含紧急解放及其他控制星球）、DSS、全球事件、Episode 战役阶段、区域、特殊部队。
  五类新增公共资源共享 Status 缓存和时钟锚点；未知字段不造值。
- 新增 Steam 官方公告/新闻 ID 详情、补丁筛选、Steam-only 在线；使用公开接口、严格来源与文本清理。
- 增加新闻/公告/DSS/战役/区域/Steam补丁订阅，仍只允许已绑定的指定 QQ 私聊用户；平台额度/拒绝逻辑沿用。
  SQLite v1/v2/v3→v4 保留订阅、指纹、配对、重试/暂停和额度；保留自增 ID 高水位。
- 卡片复验改进：区域不再跨面板拆记录，控制中心目录/简报/阶段保留语义分组。
- 全量 pytest 636 passed；Ruff、pip check、61 个文件 Python 3.11 语法验证通过。
  11 类真实公共查询成功；QQ 离线探针验证 6 条文字/JPEG 回复，没有主动发送测试消息。
- 私有数据源缺失与 Discord 专属能力边界见 GWW_PARITY.md，未声称原项目全量等价或新增推送已实送。
## 中英文 Wiki 装备百科（2026-09-17）

- 根据用户截图五类装备目录完成本地静态资料：782条、137张武器图片；来源URL/revision/许可/过时提示完整保留。
- 新增百科分类/子类、每页8条、型号/中英/社区俗称/部分名/拼写候选，按群成员隔离的5分钟选择和换代失效。
- 增加QQ模式24小时定期同步，独立受管理进程、HTTP条件缓存/限速/安全图片、先校验后原子替换，失败保留旧数据。
- 用户指出中文站后重新核实真实 /zh/ 入口；优先合并132中文武器名，已有中文详情保留真实来源。
  中文站不完整部分结合完整EN资料和维护译文，当前782条4029字段/说明中文化无待译、原数值不变。
  英文NC-SA与中文SA许可按来源分别保留；图片游戏美术权利另行说明。
- 137张武器本地图片校验通过；中文AR23/AC8实际JPEG目视检查；群/C2C离线探针捕获8条文字/图片回复。
- pytest845 passed；Ruff/pipcheck/69文件Python3.11语法检查通过；手动导入与离线重建验证通过。
- 新版于2026-09-17 00:11:08 +08:00 WebSocket READY，PID28364；详情见WIKI_CATALOG_NOTES.md。
## 卡片分行修复与武器费用/详参补全（2026-09-17）

- 属性标签和值使用实际字体宽度，短字段同排、长字段上下排；武器双列及通栏简介，短词/单位完整换行。
- 补正文解锁费、债券费/页码/累计门槛及详细统计/配件表。数据保持782条/137图，394条获债券补充。
- 默认卡前置获取解锁；详参/配件独立查询，保留全部攻击分项与数值，不把信息框和详表不同口径互相覆盖。
- 精简卡片声明/ID/revision/重复出处，保留必要归属，完整研究与来源元信息仍在目录。
- 全量879tests通过；定向119项及几何9场景通过；Ruff、pipcheck、71文件3.11语法验证通过。
- 2026-09-17 00:46:57 +08:00 WebSocket READY，PID19612；数据依据见WIKI_UNLOCK_NOTES.md。
