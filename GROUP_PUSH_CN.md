# 群自我介绍与可配置主动推送

当前主动消息只支持群聊和已绑定的普通 QQ 私聊。文字子频道、频道私信支持查询，但不提供主动订阅。

## 群内命令

每条命令前 @机器人。只有群主、群管理员或维护者配置的管理用户可以更改推送。

```text
自我介绍
推送设置
开启推送
订阅 主线
订阅 防守
订阅 新闻
订阅 战况 60
订阅列表
推送设置 每日上限 10
推送设置 最短间隔 5
推送设置 默认间隔 60
暂停推送
恢复推送
取消订阅 新闻
取消订阅
```

开启推送只打开本群开关，不自动创建订阅。主线、防守、星球、新闻、公告、DSS、战役、区域和补丁在建立基线后有变化才推送；战况按分钟定时汇总，第一次等待完整间隔。原有私聊推送仍遵循指定用户绑定规则。

机器人版本更新日志会向已经开启主动消息且至少保留一个有效订阅的群按版本发送一次。它不创建或修改星球、DSS、战况等订阅；这些游戏状态仍然只在群管理员显式订阅后推送。群成员可发送 `更新日志` 或 `changelog` 查询当前版本说明。

维护者可用 `scripts/send_release_notice.py --notice data/release_notice.json` 只读预览，添加 `--send` 才实际投递。手动工具仅发送到群，并与自动推送共用版本去重和配额；重复执行不会重发已确认的版本。公告 JSON 可选 `"scopes": ["group"]` 限定只向群公告。工具会读取 QQ 官方 `bot_state` 的 `allow_proactive_msg` 作为诊断；最终是否接受消息以发送接口响应为准。`40034105` 表示主动发送无权限，不能仅凭错误码断定是开放平台设置还是群内通知开关。

同一目标的游戏消息和更新日志共用限额。默认每群滚动 24 小时最多 5 条、两次至少间隔 5 分钟、战况默认 60 分钟。每日上限可设 1～50，最短间隔 1～1440 分钟，战况间隔 30～1440 分钟。默认间隔只影响以后未明确指定分钟的战况订阅；修改已有订阅请重新发 订阅 战况 <分钟>。

暂停保留主题，恢复会重新建立基线并重置战况等待时间，不补发历史消息。QQ 主动消息权限或额度拒绝时，对应订阅自动暂停；排除平台限制后再次订阅恢复。服务范围/平台权限仍由 QQ 决定，程序配置无法代替平台授权。

订阅列表与推送设置只读状态可由群成员查看。权限来自官方群消息 author.member_role=owner/admin，缺少该字段时按普通成员处理，不根据消息文本猜测管理权限。平台未返回角色时，维护者可以配置 manager_user_ids；填写官方用户 OpenID，不是 QQ 号码。

## 维护者配置

配置文件：data/group_push.json。模板：group_push.example.json。每次操作/发送检查时读取，修改后无须重启。

- enabled：群推送功能总开关；空白示例为 `false`，仅复制模板不会开启推送。
- allowed_groups：允许的群 OpenID 列表；空列表表示允许各群管理员自行开启，仍需要群内显式订阅。
- manager_user_ids：缺少角色字段时额外允许管理的官方用户 OpenID 列表；默认空。
- default_daily_limit/default_min_gap_minutes/default_interval_minutes：可选；省略时分别使用 5 条、5 分钟、60 分钟。填写数值，不使用空字符串。

配置损坏或缺失时拒绝群推送。群设置按机器人 AppID 和群 ID 隔离保存，换机器人不会自动继承另一个 AppID 的开启状态；不会把用户在命令里写的群号当成投递目标。

测试使用替身发送器与虚拟时间，不能据此断言生产消息已送达。群仅开启开关而没有启用订阅时，不在当前更新日志投递目标列表中；如希望只收更新日志且不保留游戏订阅，当前版本尚无独立的群公告订阅入口。

## 入群介绍

GROUP_ADD_ROBOT 事件自动发送自我介绍；欢迎语包含基础查询、快照私聊同步、候选选择和群推送操作。按事件去重并持久化，重启不会群发欢迎语；已在群里的机器人可用 自我介绍 手动查看。

## 相关说明

欢迎去重与二维码见 [欢迎说明](WELCOME_QR_CN.md)，查询选择见 [百科修复记录](WIKI_SELECTION_FIX_CN.md)。这些查询功能不创建游戏订阅。

公告文件 `version`、`title`、`body` 均须填写；空白示例不会发布公告。同一版本投递成功后修改正文不会重新发送；新公告使用新版本。明确的暂时失败可重试，永久拒绝或发送结果不明不会自动重复投递。
